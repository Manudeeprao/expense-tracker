package com.expense.tracker.config;

// The custom JavaMailSender bean was removed to allow Spring Boot's
// MailAutoConfiguration to create and configure JavaMailSender using
// properties defined in application.properties (spring.mail.*).

public class MailConfig {
    // Intentionally left empty. Configuration is handled via properties.
}
